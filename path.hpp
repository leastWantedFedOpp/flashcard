#ifndef path_hpp
#define path_hpp

#include <stdio.h>

extern std::filesystem::path root;
extern std::filesystem::path userFiles; //list of files that belong to users
extern std::filesystem::path userList; //list of users
extern std::filesystem::path userData; //throw all users .txt fils here.

#endif /* path_hpp */
